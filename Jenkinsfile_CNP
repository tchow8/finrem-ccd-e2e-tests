#!groovy

properties([
        parameters([
                choice(name: 'JourneyType', choices: 'e2eHwfJourney\ne2ePbaJourney\ntest-e2e', description: 'Different journeys to build in')
        ]),

        [$class: 'GithubProjectProperty', projectUrlStr: 'https://github.com/hmcts/finrem-ccd-e2e-tests.git'],
        pipelineTriggers([[$class: 'GitHubPushTrigger']])
])

@Library("Infrastructure")

def product = "finrem-ccd"
def component = "e2e"

withPipeline("nodejs", product, component) {

    node() {
        ws('workspace/finrem-ccd-e2e-tests') {

            stage('Checkout') {
                deleteDir()
                checkout scm
            }

            stage('Journey Tests') {
                try {
                    sh 'yarn install'
                    sh """ yarn run ${params.JourneyType} """
                }
                catch (Exception err) {
                    currentBuild.result = 'UNSTABLE'
                    echo "RESULT: ${currentBuild.result}"
                } finally {
                    sh 'ls -lart'
                    sh 'ls -lrt ./functional-output'
                    archiveArtifacts 'functional-output/*'
                    sh 'rm yarn.lock'

                }

            }

            stage('After run') {
                echo "finished"
            }

        }
        deleteDir()
    }

}